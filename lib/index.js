"use strict";

/**
 * currentYear
 * Get the current year.
 *
 * @name currentYear
 * @function
 * @param {Number} a Param descrpition.
 * @param {Number} b Param descrpition.
 * @return {Number} Return description.
 */
module.exports = function currentYear (a, b) {
    return a + b;
};